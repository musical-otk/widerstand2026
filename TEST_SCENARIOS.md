# 쿠폰 시스템 테스트 시나리오 (widerstand2026)

40%·증빙패스권·더블적립권 신규 쿠폰 추가 및 관람 기록 연동에 대한 테스트 시나리오.
Playwright로 실제 앱 함수(`saveManualCoupon`, `saveRecord`, `saveCouponPack` 등)를
직접 호출해 검증했다.

테스트 스크립트: `test-coupons.js`, `test-doublestamp.js` (세션 스크래치패드, 재사용 시 재작성 필요)

마지막 검증: 2026-07-11 — **42개 시나리오 전부 통과**

---

## A. 초기 렌더링

| # | 시나리오 | 결과 |
|---|---|---|
| A1 | 쿠폰탭 진입 시 카드 3개(50%·40%·증빙패스) 렌더링 | ✅ PASS |
| A2 | 더블적립권 인라인 행 렌더링 (카드 아래 좁은 한 줄) | ✅ PASS |
| A3 | 초기 수량 4종 모두 0 | ✅ PASS |

## B. 수동 추가/차감/복원 패널

| # | 시나리오 | 결과 |
|---|---|---|
| B4 | 40% 할인권 획득 +2 → 최종·현재 수량 모두 2 | ✅ PASS |
| B5 | 50% 할인권 획득 +1 → 최종·현재 수량 1 | ✅ PASS |
| B6 | 더블적립권 획득 +1 → 최종 수량 1 | ✅ PASS |
| B7 | 40% 할인권 "사용" → 2→1 차감 | ✅ PASS |
| B8 | 40% 할인권 "복원" → 1→2 복원 | ✅ PASS |

## C. 관람 기록 저장 → 할인권 차감

| # | 시나리오 | 결과 |
|---|---|---|
| C9 | 40% 할인권 선택 시 차액지불/증빙패스 그룹 표시 | ✅ PASS |
| C10 | 증빙패스권 사용 체크박스 존재 확인 | ✅ PASS |
| C11 | 40% 선택 + 차액지불 미체크로 저장 → 40% 2→1 차감 | ✅ PASS |
| C12 | 차액지불 체크 후 저장 → 할인권 미차감 (수량 유지) | ✅ PASS |
| C13 | 기록 삭제 → 40% 할인권 자동 복원 | ✅ PASS |

## D. 증빙패스권 연동

| # | 시나리오 | 결과 |
|---|---|---|
| D14 | 50%/40% 선택 + 증빙패스 체크 저장 → 증빙패스 1개 차감, 할인권 미차감 | ✅ PASS |
| D15 | 증빙패스 체크 기록 삭제 → 증빙패스 자동 복원 | ✅ PASS |

## E. 더블적립권 도장 수량 연동

| # | 시나리오 | 결과 |
|---|---|---|
| E16 | 도장 수량 1개 선택 시 더블적립 체크박스 행 숨김 | ✅ PASS |
| E17 | 도장 수량 2개 선택 시 더블적립 체크박스 행 표시 + 보유 수량 표시 | ✅ PASS |
| E18 | 도장 수량 3개 선택 시에도 더블적립 행 표시 (≥2 조건) | ✅ PASS |
| E19 | 더블적립 체크 후 저장 → 더블적립 2→1 차감, `doubleStampUsed=true` 필드 저장 | ✅ PASS |
| E20 | 기록 수정 폼 열기 시 더블적립 체크 상태 복원 | ✅ PASS |
| E21 | 수정 폼에서 더블적립 체크 해제 후 저장 → 1→2 복원 | ✅ PASS |
| E22 | 더블적립 사용 기록 삭제 → 더블적립 자동 복원 | ✅ PASS |

## F. 쿠폰팩 패널

| # | 시나리오 | 결과 |
|---|---|---|
| F23 | 쿠폰팩 패널에 "1주차 쿠폰팩" 라벨 표시 | ✅ PASS |
| F24 | 쿠폰팩 패널에 "3주차 쿠폰팩" 라벨 표시 | ✅ PASS |
| F25 | 1주차 팩 추가 → 40% +2, 증빙패스 +2 적용 | ✅ PASS |

## G. 기존 기록 소급 스캔

| # | 시나리오 | 결과 |
|---|---|---|
| G26 | 스캔 실행 시 차액지불+증빙패스 동시 체크 기록 감지 → confirm 후 교정 | ✅ PASS |
| G27 | 같은 날 2회 관람 기록 → 스캔 시 팩 2건 소급 적용 (날짜 중복 제거 안 됨) | ✅ PASS |

## H. 도장판 연동 해제 시 더블적립 처리 (eoduk K22 대응)

| # | 시나리오 | 결과 |
|---|---|---|
| H28 | 더블적립 체크 상태 유지 → 도장판 select를 ''로 변경 시 doubleStamp 체크박스 자동 언체크 | ✅ PASS |

`onRecStampBoardChange()` → qty 1 리셋 → `updateRecQtyLabel()` → `!show → dsCb.checked=false` 흐름으로 처리됨.
저장 시 `doubleStampUsed=false`가 되므로 쿠폰 복원 보장.

## I. 더블적립 재저장 정합성 (eoduk C6 대응)

| # | 시나리오 | 결과 |
|---|---|---|
| I29 | doubleStampUsed=true 기록을 무변경으로 재저장 → 수량 중복 차감 없음 | ✅ PASS |

eoduk2026은 saveRecord가 매번 이력을 push해서 중복 차감 버그가 있었음. widerstand2026은
`doubleStampUsed` 필드를 record에 직접 저장하고 `calcCouponCount`가 in-place filter로
집계하므로 구조상 중복 불가.

## J. 더블적립 다중 기록 누적 (eoduk N28 대응)

| # | 시나리오 | 결과 |
|---|---|---|
| J30 | 두 기록이 각각 doubleStampUsed=true → 수량 누적 차감 정확 (3-2=1) | ✅ PASS |
| J31 | 더블 미사용 기록 추가 → 수량 변화 없음 | ✅ PASS |

## K. 증빙패스 편집 해제 복원 (eoduk D9 편집 경로 대응)

| # | 시나리오 | 결과 |
|---|---|---|
| K32 | eungBingPassUsed=true 기록 저장 → 편집으로 false 변경 저장 → 증빙패스 수량 복원 | ✅ PASS |
| K33 | 증빙패스 사용 기록에서 50% 할인권 미차감 확인 (calcCouponCount ③ 필터 확인) | ✅ PASS |

## L. 보유 0개 상태에서 더블적립 체크 저장 (eoduk G13 대응)

| # | 시나리오 | 결과 |
|---|---|---|
| L34 | doubleStamp 0개에서 체크 후 저장 → Math.max(0,...) 음수 방지, 표시 수량 0 | ✅ PASS |
| L35 | 0개 선차감 상태에서 +1 획득 → 선차감이 먹혀 수량 0 (예약 소진) | ✅ PASS |

**설계 참고**: eoduk2026은 보유 0개면 체크박스를 아예 비노출하지만, widerstand2026은
qty≥2면 항상 체크박스를 표시하고 "(보유 N개)"로 안내함. 보유 0개에서 체크 후 저장하면
쿠폰이 선차감(예약 소진)되는 효과가 있음. `saveRecord()`에 doubleStamp 잔여 수량 체크가
없으므로 UI 안내("보유 0개")에 의존하는 구조.

---

## 발견 및 수정된 버그

### 🐛 B1. 기존 기록 스캔 — 날짜 기준 중복 제거 (2026-07-11)

같은 날 낮+밤 2회 관람했을 때 `scanPastRecordsForCouponPacks`가
`seen.has(date)` 로직으로 날짜당 1팩만 적용했음. 기록 수 vs 이미 적용된
팩 수를 비교하는 카운트 방식으로 교체해 N회 관람 → N팩 적용되도록 수정.
(commit `1dae75b`)

### 🐛 B2. 증빙패스 사용 시 50%/40% 할인권도 같이 차감 (2026-07-11)

`calcCouponCount` ③번 필터가 `!r.paidDiff`만 체크하고 `!r.eungBingPassUsed`를
누락해, 증빙패스 체크한 기록도 할인권 차감 대상으로 잡혔음. 필터에
`!r.eungBingPassUsed` 조건 추가. (commit `a0d7eb1`)

### 🐛 B3. 차액지불+증빙패스 동시 체크 허용 (2026-07-11)

두 체크박스가 서로 배타적이지 않아 동시에 체크 가능했음. 각 체크박스의
`onchange`에서 상대방을 강제 해제하도록 처리. (commit `f1e7588`)

### 🐛 B4. 증빙패스 체크 상태로 저장 시 차단 (2026-07-11)

`saveRecord()`의 쿠폰 잔여 체크가 `!paidDiff`만 보고 `!eungBingPassUsed`를
누락해, 50%/40% 쿠폰 0개인 상태에서 증빙패스로 처리하려 해도 "현재 보유한
쿠폰이 없습니다"로 저장이 막혔음. 체크 조건에 `!eungBingPassUsed` 추가.
(commit `582be12`)

### 🐛 B5. 더블적립 사용 버튼 — 적립 예정 있어도 0개면 차단 (2026-07-11)

`useDoubleStamp()` 버튼이 `localDate()` 기준 현재 수량으로 체크해, 미래
날짜 적립 예정이 있어도 현재 0개면 사용 불가였음. 최종 수량(`asOf=null`)
기준으로 변경. (commit `58c7c50`)

### 🐛 B6. 배역 탭 접어도 필터 안 풀림 → 캘린더 회차 칩 실종 (2026-07-14)

`selectCalRole()`이 배역 탭의 펼침/접힘(`calExpandedRoles`)만 토글하고
실제 필터값(`calFilter[role]`)은 그대로 둬서, 배우를 선택한 뒤 탭을
접어도 필터가 계속 살아있었음. 서로 양립 불가능한 조합(예: 아벨-이한솔 +
프레드릭-김준식, 실제 스케줄에 존재하지 않는 조합)이 걸리면 캘린더에
회차 칩(예매확정 체크 표시)이 전부 사라져 버그처럼 보임. 탭을 접을 때
`calFilter[role] = null`로 초기화하도록 수정.

Playwright로 `selectCalRole`/`setCalFilterFromTab` 직접 호출 검증:
아벨=이한솔, 프레드릭=김준식 선택 → 프레드릭 탭 접기 →
`calFilter.fredrick === null`, `calFilter.abel === '이한솔'` 유지,
7월 캘린더 회차 칩 0개 → 3개로 복구 확인. PASS.

---

## 아직 다루지 않은 영역 (후속 후보)

- 쿠폰팩 자동 추가(`autoAddCouponPack`) confirm 수락/거부 플로우
- 증빙패스 가용 수량 표시(`rec-eungbing-avail`)가 편집 폼에서 정확한지
- 3주차 팩(50%+증빙+더블) 소급 적용 시나리오
- 더블적립 인라인 사용 버튼 — 미래 적립 예정만 있을 때 사용 후 최종 수량 음수 방지
- 보유 0개에서 doubleStamp 체크 저장 시 경고 없이 선차감 허용 여부 (현재: UI 안내만, 저장 차단 없음)
